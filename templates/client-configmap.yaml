apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "client.configmap.name" . }}
  labels:
    app: {{ include "client.name" . }}
    release: {{ .Release.Name }}
    component: client-service
data:
  # Spring Configuration - matching your application-prod.yml
  spring.profiles.active: {{ .Values.clientService.config.springProfilesActive | quote }}
  spring.application.name: {{ .Values.clientService.config.applicationName | quote }}
  client.service.port: {{ .Values.clientService.containerPort | quote }}
  
  # MySQL Configuration - Dynamic detection
  mysql.host: "{{ include "mysql.dns.fqdn" . }}"
  mysql.port: "{{ include "mysql.port" . }}"
  database.name: {{ .Values.clientService.config.database.name | quote }}
  
  # JPA/Hibernate configuration
  jpa.hibernate.ddl.auto: {{ .Values.clientService.config.jpa.hibernateDdlAuto | quote }}
  
  # Logging configuration
  logging.level.org.hibernate.sql: {{ .Values.clientService.config.logging.hibernateLevel | quote }}
  logging.level.org.springframework: {{ .Values.clientService.config.logging.springLevel | quote }}
  
  # Eureka Configuration - matching your client application-prod.yml
  eureka.host: "{{ include "eureka.dns.fqdn" . }}"
  eureka.port: "{{ .Values.eureka.service.port }}"
  eureka.client.service-url.defaultZone: "http://{{ include "eureka.servicename" . }}:{{ .Values.eureka.service.port }}/eureka/"

  eureka.instance.preferIpAddress: "true"
  eureka.instance.hostname: "{{ include "client.fullname" . }}"

  
  {{- range $key, $value := .Values.clientService.config.additional }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
